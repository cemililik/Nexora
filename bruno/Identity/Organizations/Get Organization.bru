meta {
  name: Get Organization
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/identity/organizations/{{organizationId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
