meta {
  name: Remove Member
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/identity/organizations/{{organizationId}}/members/{{userId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
