meta {
  name: Activate Module
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/identity/tenants/{{tenantId}}/modules/crm/activate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
