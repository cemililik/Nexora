meta {
  name: Uninstall Module
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/identity/tenants/{{tenantId}}/modules/crm
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
