meta {
  name: List Activities
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/contacts/contacts/{{contactId}}/activities
  body: none
  auth: bearer
}

params:query {
  ~moduleSource:
  ~take:
}

auth:bearer {
  token: {{accessToken}}
}
