meta {
  name: Get Preferences
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/contacts/contacts/{{contactId}}/preferences
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
