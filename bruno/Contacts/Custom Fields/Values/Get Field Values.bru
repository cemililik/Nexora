meta {
  name: Get Contact Field Values
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/contacts/contacts/{{contactId}}/custom-fields
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
