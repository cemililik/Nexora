meta {
  name: Delete Field Definition
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/contacts/contacts/custom-fields/{{customFieldDefinitionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
