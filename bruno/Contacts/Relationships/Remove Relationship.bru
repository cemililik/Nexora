meta {
  name: Remove Relationship
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/contacts/contacts/{{contactId}}/relationships/{{relationshipId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
