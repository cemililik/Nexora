meta {
  name: List Relationships
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/contacts/contacts/{{contactId}}/relationships
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
