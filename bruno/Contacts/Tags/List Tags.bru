meta {
  name: List Tags
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/contacts/tags
  body: none
  auth: bearer
}

params:query {
  ~category:
  ~isActive:
}

auth:bearer {
  token: {{accessToken}}
}
