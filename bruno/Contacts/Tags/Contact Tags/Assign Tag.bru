meta {
  name: Assign Tag to Contact
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/contacts/contacts/{{contactId}}/tags/{{tagId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
