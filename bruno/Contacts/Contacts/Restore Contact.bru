meta {
  name: Restore Contact
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/contacts/contacts/{{contactId}}/restore
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
