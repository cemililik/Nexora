meta {
  name: List Contacts
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/contacts/contacts?page=1&pageSize=20
  body: none
  auth: bearer
}

params:query {
  page: 1
  pageSize: 20
  ~search:
  ~status:
  ~type:
  ~tagId:
}

auth:bearer {
  token: {{accessToken}}
}
