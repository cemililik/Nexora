meta {
  name: Get Template
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/notifications/templates/{{notificationTemplateId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
