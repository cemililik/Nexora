meta {
  name: Cancel Schedule
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/api/v1/notifications/schedule/{{notificationScheduleId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
