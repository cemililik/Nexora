meta {
  name: Delete Dashboard
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/reporting/dashboards/{{dashboardId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
