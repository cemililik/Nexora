meta {
  name: Get Widget Data
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/reporting/dashboards/{{dashboardId}}/widgets/{{widgetId}}/data
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
