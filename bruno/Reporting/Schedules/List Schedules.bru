meta {
  name: List Schedules
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/reporting/schedules
  body: none
  auth: bearer
}

params:query {
  page: 1
  pageSize: 20
  ~definitionId: {{reportDefinitionId}}
}

auth:bearer {
  token: {{accessToken}}
}
