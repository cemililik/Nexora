meta {
  name: Revoke Access
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/documents/documents/{{documentId}}/access/{{accessId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}
