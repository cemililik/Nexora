# T-011: MigrationRunner.MigrateAllModulesAsync implementation

- **ID:** T-011
- **Status:** Not started
- **Owner:** TBD
- **Phase:** phase-2
- **Milestone:** A
- **Created:** 2026-04-23
- **Last updated:** 2026-04-23

## Context

Schema-per-tenant migration orchestration requires a `MigrationRunner` that applies
every module's EF Core migrations to a tenant schema in module-dependency order.
Currently each module has its own one-off `MigrateAsync` entry point; there is no
platform-level runner that iterates tenants and applies migrations consistently.

See `docs/operations/migration-orchestration.md` §2 for the full design.

## Acceptance criteria

- [ ] `MigrationRunner.MigrateAllModulesAsync(tenantId, ct)` applies every registered
      `IModule` migration head to the tenant schema in dependency order.
- [ ] Per-tenant `pg_advisory_lock` prevents concurrent migrations for the same tenant.
- [ ] Failure marks the tenant `migration_failed` and logs to
      `platform_migration_failures` table.
- [ ] Unit + integration tests (Testcontainers Postgres).

## Links

- Runbook: `docs/operations/migration-orchestration.md`
- Related ADRs: ADR-0002 (Multi-Tenancy), ADR-0003 (Deployment Strategy)

## Status log

- 2026-04-23 — Not started — Stub created for traceability (reserved in migration-orchestration runbook §2).
