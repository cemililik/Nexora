# T-017: Notifications `BodyRendered` nullable migration

- **ID:** T-017
- **Status:** Not started
- **Owner:** TBD
- **Phase:** phase-1.5
- **Milestone:** C (follow-up to T-004)
- **Created:** 2026-04-23
- **Last updated:** 2026-04-23

## Context

T-004 WP3 Notifications handler for `ContactGdprDeletedIntegrationEvent` scrubs
`Notification.BodyRendered` to the placeholder `[REDACTED]` instead of `null`.
The Notifications SPEC §PII retention policy (added 2026-04-22) specifies that
hot retention ends at 30 days and cold storage sets `body_rendered` to `null` —
but the current column is `IsRequired()` (non-nullable), so we cannot honor the
`null` contract without a schema change.

The placeholder approach works operationally (no PII leaks) but creates a minor
SPEC drift: a GDPR audit question "why is the body column still populated?" has
to be answered with "it's the placeholder, not real content" — a weak answer when
compared to "it's null".

## Acceptance criteria

- [ ] Migration: `ALTER TABLE notifications_notifications ALTER COLUMN body_rendered
      DROP NOT NULL;`
- [ ] EF configuration: `builder.Property(n => n.BodyRendered).IsRequired(false);`
- [ ] `Notification.ScrubRenderedBody()` sets `BodyRendered = null` instead of
      `PiiRedactedPlaceholder.Value`.
- [ ] Existing anonymize/hard-delete tests updated to assert `null` not
      placeholder.
- [ ] Recurring job `notifications:purge-rendered-bodies` (declared in SPEC, may not
      yet be implemented) also sets to `null` at end of hot-retention window.
- [ ] SPEC updated to remove the placeholder note.

## Technical notes

- Migration is additive (drops NOT NULL — widens domain). Safe for existing rows.
- The purge job is a separate deliverable; this task only unblocks it.
- Coordinate with any analytics / reporting queries that may assume non-null
  `body_rendered` — currently there are none in Reporting spec.

## Links

- Related: T-004 (WP3 scrub path that currently writes placeholder)
- Blocks: Notifications PII retention compliance section in production readiness.

## Status log

- 2026-04-23 — Not started — Filed as T-004 follow-up per maintainer review (WP3
  deviation noted).
