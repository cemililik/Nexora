# T-010: Cross-module audit payload PII scan for GDPR erasure

- **ID:** T-010
- **Status:** Ready (ADR-0026 Accepted 2026-04-24 — implementation gate open)
- **Owner:** cemililik
- **Phase:** phase-1.5 → carries over to Phase 2 Milestone A/B (see 2026-04-24 status log for the reclassification rationale)
- **Milestone:** C (follow-up to T-004)
- **Created:** 2026-04-23
- **Last updated:** 2026-04-24

## Context

T-004 WP4 Audit inbox handler for `ContactGdprDeletedIntegrationEvent` redacts audit
entries matched via `EntityType = "Contact" AND EntityId = <contactId>` — the indexed
path. It explicitly does NOT scan other modules' audit entries (CRM Lead, Documents
Document, Subscription Subscription, etc.) whose `BeforeState` / `AfterState` JSON
columns may contain the erased contact's name/email interpolated into payload
(e.g. "Lead assigned to John Doe <john@example.com>").

This is a **GDPR Article 17 compliance gap**: PII must be erased wherever it appears,
not just where `EntityType=Contact`. The T-004 handler acknowledges this limitation
in its source comment:

> "Payload-scan matches are not reliable across free-form JSON columns."

This task delivers the payload-scan redaction strategy, filed as a follow-up because
it requires cross-module design work (how do modules declare which audit payload
fields carry contact references?) that is out of T-004's scope.

## Acceptance criteria

- [ ] Each module that logs audit entries referencing contacts exposes a
      `IContactReferenceLocator` implementation declaring which JSON payload paths
      may contain contact IDs (e.g. `$.assignedTo.contactId`, `$.owner.contactId`).
- [ ] New `ContactPayloadScanJob` (Hangfire, queue `maintenance`) triggered by
      `ContactGdprDeletedIntegrationEvent` scans audit entries module-by-module using
      each locator's declared paths.
- [ ] Scan produces an additional audit entry with `action = "gdpr_erasure_scan"`
      summarizing per-module rows redacted.
- [ ] False-positive rate measured: documented test suite verifies the scan does not
      redact unrelated rows.
- [ ] Performance target: scan completes within 15 min for a tenant with 10M audit
      rows (benchmarked in integration test against seeded fixture).
- [ ] SPEC updated: `docs/modules/tier-1-core/audit/SPEC.md` §PII retention section.
- [ ] Audit coverage standard updated: `docs/standards/audit-coverage.md` §3 Audit
      row notes the scan obligation.

## Technical notes

- Consider `JsonPath.NET` (`System.Text.Json.JsonPath` preview or `json-everything`)
  for locator path resolution.
- Scan MUST be idempotent — re-running after partial completion must not duplicate
  redaction markers. Inbox guard pattern + per-entry `redacted_at` timestamp.
- CRM, Documents, Subscription, Fundraising, Finance are candidate consumers at
  module registration.

## Links

- Blocked by: none (T-004 is the producer)
- Blocks: NMP tenant provisioning gate for EU tenants (ADR-0023) when coupled with
  retention matrix audit passes.
- Related: ADR-0008 GDPR Deletion Strategy, T-004 (parent)

## Status log

- 2026-04-23 — Not started — Filed as T-004 follow-up after maintainer review
  identified cross-module PII leak in WP4.
- 2026-04-23 — Blocked — Before kicking off cross-module implementation, the
  design choice (per-module `IContactReferenceLocator` vs. full-table regex
  scan vs. write-time structured tagging vs. no-op) was captured in
  **ADR-0026** (Proposed). The ADR recommends Option 1 (per-module locator
  with declared JSON paths) because it is the only choice that makes
  false-positive redaction impossible by construction while keeping the
  audit module generic. Task resumes once maintainer promotes ADR-0026 to
  Accepted; implementation work then includes the locator infrastructure,
  the Hangfire scan job, a coverage architecture test, and a 10M-row
  performance benchmark (see ADR-0026 §Implementation notes).
- 2026-04-24 — Ready — maintainer — ADR-0026 promoted to **Accepted**
  (commit c03ada3). Implementation gate is open. **Not picked up in the
  Phase 1.5 carry-over sweep** because: (a) the AC wants locators across
  5+ modules, but four of them (CRM, Subscription, Fundraising, Finance)
  don't exist yet — they ship in Phases 2 and 3a. (b) The 10M-row
  performance target needs a seeded-fixture benchmark harness that doesn't
  exist in the repo today. (c) A realistic CR for this task requires
  module-owner reviews from each consuming module, which is outside a
  solo sprint.

  **Reclassification: T-010 carries over to Phase 2 Milestone A/B.** The
  Contacts-module locator + Audit-module scan job + architecture test can
  ship as a T-010 scaffolding task in Phase 2 Milestone A (alongside the
  CRM pilot, so CRM is the first real cross-module consumer of the
  locator contract). Subscription / Finance / Fundraising locators land
  with those modules in their respective phases. Update `current.md`
  carry-over list + `phase-2-enterprise.md` Milestone A scope when the
  maintainer confirms.

  **Until reclassification, T-010 stays filed under Phase 1.5 with
  Status: Ready** — an operator who onboards a tenant to production now
  accepts the documented Article 17 gap in T-004's source comment; the
  Audit module's existing `EntityType = "Contact"` indexed path catches
  the common case, and the gap is quantified (payload-embedded PII
  across the four non-existent modules is trivially empty today).
